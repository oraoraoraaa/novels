# AGENTS.md

本文件适用于整个仓库。这里是小说写作工程，不是软件产品仓库；任何改动都应优先保证剧情连续性、人物一致性和可审阅的文本差异。

## 开始工作前

1. 阅读根目录 `README.md`、目标书册的 `README.md`，以及该书册 `outline/` 中当前版本的主控大纲。
2. 运行 `git status --short`。不要覆盖、回滚或顺手整理不属于当前任务的改动。
3. 用户没有明确要求创作正文时，不要补写场景、对白、心理描写或世界观真相。

## 资料依据与冲突处理

用户已经明确指定的修改直接执行，并同步相关正文、大纲与写作记录，不对同一决定重复询问。其余资料的阅读与核对顺序为：

1. 用户在当前任务及此前任务中仍然有效的明确要求。
2. 已写章节正文及用户已经确认的事实。
3. `outline/` 中当前版本的主控大纲、相关连续性细节库与写作记录。
4. 旧版本大纲、其他笔记和推测。

这个顺序不用于自动裁决剧情冲突。大纲可能存在错误，不能仅凭版本号或“大纲优先”覆盖前文；前文与后续大纲不一致时，也不能自行决定放弃后续设定。

大纲中标记为“角色判断”“尚未验证”“仍未知”的内容，不得在旁白或说明中擅自升级为客观事实。固定数字、系统字符串、时间线、人物称谓、同意边界和伦理规则必须保持一致。

## 跨章节连续性检查与暂停规则

- 开始续写前和完成草稿后，核对前面已写章节、当前章大纲、后续章节的大纲或正文，以及相关连续性细节库。至少检查紧邻前后章；金额、伤势、人物弧线、系统能力和伏笔等跨章事项应追溯到实际关联的章节。
- 检查事件先后与时间间隔、人物位置和知情范围、伤势与恢复、关系进展、物件状态、金额与币种、技术上线时间、权限及因果链。写作记录只是索引，不能代替回读相关正文和大纲。
- 角色口头上无关紧要的要求、玩笑式命令或临时约定，不自动视为持续有效的设定。作者已授权根据人物、语境和叙事需要自行取舍、淡化或忽略，不必逐句兑现或为此暂停。这个裁量不用于改写伤势、事件先后、人物知情范围等实质事实，也不替代作者对关键剧情的决定。
- 除上述已授权灵活处理的口头要求外，如果后面的内容与前文逻辑不符、不一致，或大纲自身的冲突会影响续写，立即暂停正文写作。向用户说明冲突双方的出处、具体差异和受影响的情节，询问应采用哪一个版本；在得到答复前不得继续正文。
- 不得通过擅改前章、删除冲突细节、补造幕后事件、默认采用最新大纲或自行调换事件顺序来消除冲突。可以继续处理用户已明确授权且与冲突无关的修改。
- 用户决定后，将采用的版本及其影响同步到相关正文、大纲和写作记录，再恢复写作。对用户已明确解决的冲突，不重复请求确认。

## LaTeX 组织

- 每册的 `main.tex` 是唯一编译入口，只负责文档结构和文件装配。
- 通用排版设置放在 `tex/preamble.tex`，书名等元数据放在 `tex/metadata.tex`。
- 每个正式章节单独放在 `chapters/`；文件名使用两位数字排序，终章使用 `epilogue.tex`。
- 标题页等前置内容放在 `frontmatter/`；剧情大纲和连续性资料放在 `outline/`。
- 不要提交 `build/` 或 LaTeX 辅助文件。不要把完整正文重新合并进 `main.tex`。
- 中文正文使用 UTF-8；默认通过 XeLaTeX 和 `ctexbook` 编译。

## 写作与编辑边界

- 面向成年读者写作。保留人物的欲望、粗粝语言、矛盾和行为后果；不要为了表现人物“正确”而把对白写成规则宣讲。亲密、冲突与创伤描写的分量由叙事需要决定。
- 文学性通过具体可感的动作、环境、心理层次、侧面描写和结构回声形成；避免堆砌比喻、空泛金句、梗式旁白或套用网文节奏。段落长短应随场景变化。
- 每次集中完成一个可审阅的章节单元，并做事实、视角、语言三轮自查。不得为了推进进度擅自补齐未知设定或提前透支后续人物成长。
- 开始续写前查看目标书册的 `outline/writing-notes.md`（若存在）。该文件记录草稿状态与衔接细节，不替代主控大纲；未经用户确认的扩写细节仍属草稿。
- 保持大纲规定的 9 章 + 终章结构，除非用户明确要求调整。
- 主人/狗狗关系必须体现自愿、可停止与照顾，不得把伤害或忍痛写成忠诚证明。
- 涉及绑架、暴力与创伤时不得情色化，也不得让受害者承担“被污染”的叙事。
- 綾的名字、人格、记忆、未来身体及亲密互动均遵循其自主选择；复制实例具有大纲规定的伦理地位。
- 技术和悬疑段落优先展示少量关键证据，再由人物解释；避免用整页日志代替叙事。
- 修改正文时同样执行跨章节连续性检查与暂停规则，不把检查范围限于当前段落或当前章。

## 校验

用户要求暂不进行 LaTeX 编译时，只做文本、连续性和源文件结构检查，不运行编译命令或为此安装工具链；明确记录尚未进行排版验收。

未被要求跳过编译且环境具备 TeX Live 时，于目标书册目录运行：

```sh
latexmk -xelatex main.tex
```

至少检查：编译成功、无缺失引用或文件、目录顺序正确、生成 PDF 中中文与日文字符正常显示。若本机没有工具链，应明确说明只完成了静态结构校验。

## Git

- 提交信息使用 Conventional Commits，例如 `docs: clarify continuity rules`、`feat: draft chapter 01`、`fix: correct timeline inconsistency`。
- 一次提交只包含一个清晰目的；不要在小说内容提交中混入无关格式化。
- 提交前检查 `git diff --check` 与 `git status --short`，并执行与本次改动相称的校验。
